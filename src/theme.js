import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#B02EFF', // Light shade of your primary color
      main: '#BE6FFF', // Main shade of your primary color
      dark: '#7B008A', // Dark shade of your primary color
      darker: '#1D1B1D', // Darker shade of your primary color
    },
  },
});

export default theme;
